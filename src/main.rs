fn main() {
    println!("The main file does not contain any examples.");
    println!("Run examples files from the src/bin/ directory with 'cargo run --bin <file_name>' excluding the .rs extension");
}
